{
  "name": "team-auction",
  "version": "1.0.0",
  "description": "Team Auction System",
  "main": "server.js",
  "scripts": {
    "setup": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server.js",
    "postinstall": "npm run setup && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.x"
  }
}